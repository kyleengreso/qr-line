<?php
// TESTER: aceday

if ($_SERVER["REQUEST_METHOD"] == "POST") {
    // Maintenance for the system

    if (isset($_GET['counter'])) {
        // Recalculate the queue number for the counter
    } 
}